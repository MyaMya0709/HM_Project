using UnityEngine;

public enum ItemType
{
    Exp,
    Gold,
    Heart,
    Buff,
    Skill
}

public enum EnemyMoveType
{
    Ground,
    Sky
}

public enum SlotType
{
    StatUP,
    WeaponGet,
    WeaponUP
}

public enum StatType
{
    moveSpeed,
    attackPower,
    attackSpeed
}