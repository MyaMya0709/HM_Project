using UnityEngine;

public enum ItemType
{
    Exp,
    Gold,
    Heart,
    Buff,
    Skill
}

public enum EnemyMoveType
{
    Ground,
    Sky
}

public enum SlotType
{
    StatUP,
    ManualWeapon,
    AutoWeapon
}

public enum StatType
{
    moveSpeed,
    attackPower,
    attackSpeed
}